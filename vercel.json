{
  "buildCommand": "npm run build && cd api && npm install && npx prisma generate",
  "outputDirectory": "dist/public",
  "installCommand": "npm install && cd api && npm install",
  "functions": {
    "api/biasradar/*.py": {
      "maxDuration": 60
    },
    "api/*.js": {
      "maxDuration": 10
    }
  },
  "rewrites": [
    {
      "source": "/((?!api).*)",
      "destination": "/index.html"
    }
  ]
}