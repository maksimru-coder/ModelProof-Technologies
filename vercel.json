{
  "buildCommand": "cd client && npm ci && npm run build",
  "installCommand": "cd client && npm ci",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}